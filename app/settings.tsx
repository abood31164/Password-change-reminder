import { Feather } from '@expo/vector-icons';
import { router } from 'expo-router';
import { Alert, Pressable, StyleSheet, Switch, Text, View } from 'react-native';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { BrandMark } from '@/components/BrandMark';
import { useReminders } from '@/context/ReminderContext';
import { useColors } from '@/hooks/useColors';
import { useLanguage } from '@/context/LanguageContext';

export default function SettingsScreen() {
  const colors = useColors(); const insets = useSafeAreaInsets(); const { lockEnabled, setLockEnabled } = useReminders(); const { language, isRTL, setLanguage, t } = useLanguage();
  async function toggleLock(value: boolean) { const ok = await setLockEnabled(value); if (!ok && value) Alert.alert(t('biometricUnavailable'), t('biometricUnavailableBody')); }
  return <View style={[styles.screen, { backgroundColor: colors.background, direction: isRTL ? 'rtl' : 'ltr' }]}><View style={[styles.top, { paddingTop: insets.top + 12 }]}><Pressable onPress={() => router.back()} hitSlop={12}><Feather name="arrow-right" size={23} color={colors.foreground} /></Pressable><Text style={[styles.topTitle, { color: colors.foreground }]}>{t('settings')}</Text><View style={{ width: 23 }} /></View><View style={{ padding: 20 }}><View style={styles.brand}><BrandMark /><Text style={[styles.brandName, { color: colors.foreground }]}>{t('appName')}</Text><Text style={[styles.brandSub, { color: colors.mutedForeground }]}>{t('appTagline')}</Text></View><Text style={[styles.section, { color: colors.mutedForeground }]}>{t('securityTools')}</Text><Pressable testID="settings-generator-button" onPress={() => router.push('/generator')} style={[styles.tool, { backgroundColor: colors.card, borderColor: colors.border }]}><View style={[styles.settingIcon, { backgroundColor: colors.mint }]}><Feather name="key" size={18} color={colors.mintStrong} /></View><View style={styles.settingCopy}><Text style={[styles.settingTitle, { color: colors.foreground }]}>{t('passwordSuggestion')}</Text><Text style={[styles.settingBody, { color: colors.mutedForeground }]}>{t('passwordSuggestionBody')}</Text></View><Feather name="chevron-left" size={18} color={colors.mutedForeground} /></Pressable><Text style={[styles.section, { color: colors.mutedForeground, marginTop: 24 }]}>{t('privacySecurity')}</Text><View style={[styles.card, { backgroundColor: colors.card, borderColor: colors.border }]}><View style={[styles.settingIcon, { backgroundColor: colors.mint }]}><Feather name="shield" size={18} color={colors.mintStrong} /></View><View style={styles.settingCopy}><Text style={[styles.settingTitle, { color: colors.foreground }]}>{t('lockApp')}</Text><Text style={[styles.settingBody, { color: colors.mutedForeground }]}>{t('lockAppBody')}</Text></View><Switch testID="biometric-switch" value={lockEnabled} onValueChange={toggleLock} trackColor={{ false: colors.muted, true: colors.coral }} thumbColor={colors.primaryForeground} /></View><Text style={[styles.section, { color: colors.mutedForeground, marginTop: 24 }]}>{t('language')}</Text><View style={[styles.languageCard, { backgroundColor: colors.card, borderColor: colors.border }]}><Pressable testID="language-english-button" onPress={() => setLanguage('en')} style={[styles.languageButton, { backgroundColor: language === 'en' ? colors.coral : colors.secondary }]}><Text style={[styles.languageText, { color: language === 'en' ? colors.primaryForeground : colors.mutedForeground }]}>{t('english')}</Text></Pressable><Pressable testID="language-arabic-button" onPress={() => setLanguage('ar')} style={[styles.languageButton, { backgroundColor: language === 'ar' ? colors.coral : colors.secondary }]}><Text style={[styles.languageText, { color: language === 'ar' ? colors.primaryForeground : colors.mutedForeground }]}>{t('arabic')}</Text></Pressable></View><View style={[styles.note, { backgroundColor: colors.navy }]}><Feather name="info" size={16} color={colors.coral} /><Text style={styles.noteText}>{t('privacyNote')}</Text></View><Text style={[styles.section, { color: colors.mutedForeground, marginTop: 28 }]}>{t('about')}</Text><View style={[styles.about, { backgroundColor: colors.card, borderColor: colors.border }]}><Text style={[styles.aboutTitle, { color: colors.foreground }]}>{t('appName')}</Text><Text style={[styles.aboutText, { color: colors.mutedForeground }]}>{t('version')}</Text></View></View></View>;
}
const styles = StyleSheet.create({ screen: { flex: 1 }, top: { paddingHorizontal: 20, paddingBottom: 15, flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' }, topTitle: { fontSize: 17, fontWeight: '700' }, brand: { alignItems: 'center', marginTop: 15, marginBottom: 32, gap: 7 }, brandName: { fontSize: 21, fontWeight: '700' }, brandSub: { fontSize: 13 }, section: { fontSize: 12, fontWeight: '700', textAlign: 'right', marginBottom: 10 }, tool: { borderWidth: 1, borderRadius: 21, padding: 16, flexDirection: 'row-reverse', alignItems: 'center', gap: 12 }, card: { borderWidth: 1, borderRadius: 21, padding: 16, flexDirection: 'row-reverse', alignItems: 'center', gap: 12 }, settingIcon: { width: 42, height: 42, borderRadius: 14, alignItems: 'center', justifyContent: 'center' }, settingCopy: { flex: 1, alignItems: 'flex-end', gap: 4 }, settingTitle: { fontSize: 15, fontWeight: '700' }, settingBody: { fontSize: 11, textAlign: 'right', lineHeight: 17 }, languageCard: { borderWidth: 1, borderRadius: 21, padding: 8, flexDirection: 'row', gap: 8 }, languageButton: { flex: 1, minHeight: 44, borderRadius: 14, alignItems: 'center', justifyContent: 'center' }, languageText: { fontSize: 13, fontWeight: '700' }, note: { borderRadius: 19, padding: 16, marginTop: 15, flexDirection: 'row-reverse', alignItems: 'flex-start', gap: 10 }, noteText: { color: '#E7ECF5', flex: 1, textAlign: 'right', fontSize: 12, lineHeight: 19 }, about: { borderWidth: 1, borderRadius: 20, padding: 17, flexDirection: 'row-reverse', justifyContent: 'space-between' }, aboutTitle: { fontSize: 14, fontWeight: '700' }, aboutText: { fontSize: 12 } });
